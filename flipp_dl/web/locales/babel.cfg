[jinja2: **.html]
encoding = utf-8

# Route handlers translate a handful of short, non-technical messages
# themselves (e.g. the Komga "Test connection" errors) rather than
# leaving that to the template - so routes.py needs its own mapping,
# extracted separately (see README.md "Översättningar").
[python: **.py]
